# CLAUDE.md

《스페인 전람회의 그림》 — 2005년 12월 스페인 여행 기록으로 만드는 **9분 40초 심포닉 프로그레시브 뮤직비디오**. 음악은 라이선스가 아니라 **직접 작곡·합성**한다.

이 파일은 Claude Code가 세션 시작 시 자동으로 읽는다. 대화 맥락은 세션 사이에 이어지지 않으므로, **이어서 작업하려면 이 파일과 `00`·`01`·`07` 문서를 먼저 읽어야 한다.**

---

## 1. 절대 규칙

검수자(JinYong)가 명시한 규칙이다. 어기면 다시 해야 한다.

| # | 규칙 | 이유 |
|---|---|---|
| R1 | **문서는 마크다운이 기본.** docx·pdf는 파생물이며 `백업/` 아래 해당 버전 폴더에 둔다 | — |
| R2 | **파일명에 언더바 금지.** 공백과 구체적인 단어로. 문서는 `00. `·`01. ` 일련번호 접두사 | 정렬 순서가 곧 작업 순서 |
| R3 | **기존 문서 내용을 절대 삭제하지 않는다.** 판단이 바뀌면 원문을 남기고 "개정" 절을 덧붙여 **무엇이 왜** 바뀌었는지 적는다 | 변경 이력 자체가 나중에 판단의 근거가 된다 |
| R4 | **답변은 한글.** 모든 답변 끝에 서버 날짜를 확인해 `작성 일자: YYYY-MM-DD` 로 적는다 | — |
| R5 | 문서를 고쳤으면 **반드시** `python3 "99. 작업 스크립트/검증.py"` 를 돌린다 | 6번 검사가 스냅샷 누락을 자동으로 잡는다 |
| R6 | 계획·설계 문서는 **서술형**을 선호한다. 표만 나열하지 말고 왜 그렇게 하는지 쓴다 | `07. 작곡 계획`이 그 예 |

`CLAUDE.md`와 `.gitignore`는 도구가 이름을 강제하므로 R2의 예외다.

파일명에 공백이 있으므로 마크다운 링크는 `[제목]` 뒤에 괄호와 꺾쇠(`<` `>`)로 파일명을 감싸는 형태로 쓴다 — 예: 대괄호에 제목, 소괄호 안 꺾쇠에 `00. 스페인 전람회의 그림 기획서.md`.

---

## 2. 문서 체계 — 무엇이 어디 있는가

**이 표는 목차일 뿐이다. 내용을 여기에 복사하지 말 것.** 같은 사실이 두 곳에 있으면 반드시 어긋난다.

| 문서 | 무엇이 들어 있나 | 상태 |
|---|---|---|
| `00. 스페인 전람회의 그림 기획서.md` | 최상위 기획. 악장 구성표, 편성, 영상 설계(V1~V3 절대 규칙), 리스크, 게이트 G1~G7 | **G1 승인** |
| `01. 작업 분해 구조와 백로그.md` | WBS 51개 + 백로그 BL-01~BL-25, 임계경로 30.0 작업일 | 진행 |
| `02. AI 영상 생성 기술 검증 계획서.md` | G3 PoC 스파이크. 가설 H1~H7, 테스트 T1~T8 | 착수 전 |
| `03. 스토리라인과 등장인물.md` | 서사. 그녀(La Duende) 설정, 악장별 서사, 두 주제 설계 | **G1 승인** |
| `04. 문서 개정 이력.md` | 버전 대장 v0.1→v1.6, 운영 절차, 되돌리기 | 상시 갱신 |
| `05. 코드 진행 스케치 검토서.md` | 악장별 화성. **9장 = 전곡 화성 v1.4, 10장 = 유튜브 검토 영상** | 9·10장 판정 대기 |
| `06. 음악 제작 방식 결정서.md` | BL-06 판정 — 자체 합성 유지. Suno와의 차이, 제약, 재검토 조건 | 결정 완료 |
| `07. 작곡 계획.md` | WBS 1.4 전곡 작곡 설계. **서술형** | 5·8장 승인 완료 · 전곡 데모(G2) 승인 대기 |
| `08. 중간 점검과 인수인계.md` | Phase 1 회고. 판단이 바뀐 다섯 번, 남은 위험, 실무 팁 | 기록 |
| 09 · 10 (예정) | 프롬프트 규격서(G3 통과 후) · 스토리보드(Phase 3) | 미작성 |

`자료/` = 참고 악보·MusicXML. `2005년 12월 스페인/` = 원본 사진 253장 + 무음 MOV 9편.

### `98. 일일 마감/` — 작업 일지 (v1.12 신설 · v1.13 계획 추가)

위 표의 문서들과 **성격이 다르다.** 명세가 아니라 일지이므로 **프로젝트 버전 체계 밖**이다.

| 파일 | 주기 | 무엇 |
|---|---|---|
| **`YYYYMMDD-0-daily-plan.md`** | 하루 1개 · 아침 | **오늘 할 일과 왜 오늘인지.** 하지 않을 것까지 |
| `YYYYMMDD-HHMM-session-log.md` | 수시 | 사실과 숫자. **아래 둘의 원본** |
| `YYYYMMDD-daily-report.md` | 하루 1개 · 저녁 | 검수자가 읽고 판단할 것 |
| `YYYYMMDD-vibe-log.md` | 하루 1개 · 저녁 | 숫자로 안 남는 것 — 판단의 경로, 실수의 모양 |

파일명은 **영어 + `YYYYMMDD-` 접두사**다(R2의 예외 — 날짜 정렬이 곧 시간 순서다). 계획만 `-0-`을 끼우는데, 하이픈(0x2D)이 어떤 숫자보다 앞서므로 **이름순 정렬이 곧 하루의 순서**가 된다. 버전을 매기지 않고 `백업/` 스냅샷에도 넣지 않으며, `검증.py`는 루트 `*.md`만 훑으므로 이 폴더를 보지 않는다.

**"오늘 할 일"이라고 말하면 `daily-plan`이, "일일마감"이라고 말하면 `daily-closing`이 전부 처리한다.**

계획에는 규칙이 셋 있다. **① 명세를 복사하지 않고 절 번호로 가리킨다** — 2절의 경고가 일일 문서에도 적용된다. **② 틀려도 고치지 않는다** — 저녁에 결과에 맞춰 손보면 그 순간 회고 재료가 사라진다. 어긋난 자리는 데일리보고 3절과 바이브로그가 다룬다(R3를 하루 단위로 적용한 것이다). **③ 하지 않기로 한 것도 적는다** — 없으면 다음날 같은 것을 또 후보로 올린다.

---

## 3. 수정 절차

문서를 고칠 때는 이 순서를 지킨다. `04. 문서 개정 이력` 5장에 있는 절차와 같다.

```
1. 수정 전  →  백업/vN.N (날짜) 요약/  에 전체 .md 복사
2. 수정      →  기존 내용은 지우지 않고 "개정" 절을 덧붙인다 (R3)
3. 대조      →  바뀐 수치가 다른 문서에도 있으면 함께 고친다
4. 버전      →  각 문서 머리 표의 버전을 올리고 개정 이력 행 추가
5. 대장      →  04 문서 3장 전체 이력에 행 추가
6. 검증      →  python3 "99. 작업 스크립트/검증.py"
```

**버전 규칙** — `v[MAJOR].[MINOR]`. 프로젝트 전체에 버전 하나를 붙인다(문서별 버전이 아니다). MAJOR는 검수 게이트 통과 시(G1 승인 → v1.0), MINOR는 내용 변경 시. 오탈자·서식만 고쳤으면 올리지 않고 이력에 `(서식)`으로 남긴다. 되돌린 경우도 앞으로 흐르는 새 버전으로 기록한다.

문서별 버전을 쓰지 않는 이유는 문서들이 **같은 수치를 공유**하기 때문이다(사진 253장, 총 기간 30.0일 등). 개별 버전이면 "기획서 v1.2 + WBS v2.1"이 맞는 조합인지 매번 따져야 한다.

**현재 버전: v1.13** (2026-08-07). 최신 스냅샷 `백업/v1.13 (2026-08-07) 베이스 대선율 수작업/`.

MINOR가 두 자리로 넘어갔다. **`백업/` 폴더를 이름순으로 정렬하면 `v1.10`이 `v1.2`보다 앞에 온다** — 사람이 눈으로 훑을 때도, 스크립트가 최신을 고를 때도 함정이다. `검증.py`는 v1.10에서 버전 번호를 숫자로 파싱하도록 고쳤다.

---

## 4. 실행 환경

### 필요한 것

| 항목 | 비고 |
|---|---|
| Python 3 | `numpy` · `scipy` · `Pillow` |
| **ffmpeg / ffprobe** | **2026-08-07 설치 완료** — Gyan.FFmpeg 9.0 (`winget install --id Gyan.FFmpeg`). 사용자 PATH에 등록되므로 **새 셸에서만 잡힌다.** 렌더·인코딩·MP3 변환에 쓴다. 설치 직후 `05` 9.9절이 미확인으로 남겼던 v1.5 납품 mp3 실측을 마쳤고, 결론이 9.11절에 있다. ~~2026-08-06 확인 — 현재 PC에 설치돼 있지 않다~~ |
| **Git for Windows** | Windows에서 Claude Code가 Bash 도구를 쓰려면 필요하다. 없으면 PowerShell로 대체되는데, `.claude/skills/` 의 명령 예시가 전부 POSIX 셸 기준이라 그대로는 동작하지 않는다 |

첫 세션에서 아래 셋을 먼저 확인할 것.

```bash
python -c "import numpy, scipy, PIL; print('ok')"
ffmpeg -version
bash --version        # POSIX 셸이 잡히는지
```

### 주요 명령

`99. 작업 스크립트/` 안에서 실행한다.

```
python 전곡화성.py      # 전곡 9분40초 렌더 → 전곡화성.wav (+ chordlog.npy)
python 검증화성.py      # 크로마·대역·경직됨·악장별 RMS 실측
python 자막생성.py      # chordlog.npy → 자막.ass
python 음정축.py        # CQT 음정축 이미지 → 음정축.png
python 검증.py          # 문서 정합성 6종 검사  (상위 폴더에서 실행)
```

영상 렌더는 `영상필터.txt`의 필터그래프를 개행 제거해 `-filter_complex_script`로 넘긴다. 10장에 규격이 있다.

### 성능 주의

이전 작업은 2코어·메모리 3.9GB 샌드박스에서 했다. 그 제약 때문에 두 가지가 코드에 남아 있다.

- **스템을 따로 보관하지 않는다** — 7스템 × 스테레오 × 580초는 3GB다. `화성.py`의 `Desk`는 단일 스테레오 버퍼에 즉시 합산하고 스템별 **제곱합만** 누적해 페이더 전 RMS를 실측한다. 대신 **페이더를 렌더 전에 정해야 하고, 믹스만 바꾸려도 전곡을 다시 렌더해야 한다.**
- **캐시에 상한을 두고 세기·길이를 양자화한다** — 연주 흔들림이 세기를 연속적으로 흔들면 캐시 키가 매번 달라져 메모리가 터진다. `화성.py`의 `pn`·`st`·`ny`·`rick`·`hamm`·`moog`가 그 래퍼다.

PC는 메모리가 넉넉할 것이므로 스템 분리 보관으로 되돌리면 믹스 반복이 훨씬 싸진다. **다만 되돌리기 전에 메모리를 확인할 것.** 되돌리면 `Desk`의 `_put`·`stem_rms`·`mix`를 함께 고쳐야 한다.

---

## 5. 코드 지도

| 파일 | 역할 |
|---|---|
| `piano.py` | 피아노. 가산합성 + 현 비조화도 + 이중 감쇠. `note()` · `Score` · `_room()` |
| `ensemble.py` | 현악(8성부 디튠) · 나일론기타(Karplus-Strong) · 팔마스 · 카혼 · 타콘 · **리켄배커 베이스** `rick()` |
| `synth.py` | 해먼드(9드로바 + 레슬리) · 무그 · 베이스 · 드럼 · `Mix`. **무그는 되먹임 라더 `ladder_nl`** — 자기발진한다(BL-24b). 옛 `ladder_sweep`은 되먹임이 없어 발진 불가이며 `bass()`만 아직 쓴다 |
| **`화성.py`** | **화성 엔진.** 화음 사전, `voice_lead()` 성부 진행 최적화, `bassline()` 베이스 생성, `tape()` 테이프 새추레이션, `Desk` 콘솔 |
| **`전곡화성.py`** | 전곡 10악장 악보. 악장별 진행·편성·텍스처 |
| `검증화성.py` | 음원 검증 — 크로마, 조성 밖 음, 대역 균형, 경직됨 지표, 악장별 RMS, F♯→F |
| `검증무그.py` · `데모무그.py` | BL-24b — 자기발진 여부·발진 음정·진폭 안정성 검사 / 옛 필터·새 필터·비명 3구간 청취 데모 |
| `자막생성.py` · `음정축.py` | 유튜브 검토 영상의 자막(ASS)과 음정축 이미지 |
| `검증.py` | 문서 정합성. 표·링크·앵커·공유 수치·버전·**스냅샷 누락** |
| `extract.py` · `채보.py` | MusicXML 추출 / 악보 이미지 OMR (주제 확정에 사용, 완료) |
| `sketch.py` · `demo60.py` · `themes_v12.py` · `themes_v13.py` | 과거 버전 데모. 참고용이며 수정하지 않는다 |

한글 모듈명(`화성.py`)을 `import 화성` 으로 쓴다. Windows에서 인코딩 문제가 생기면 `PYTHONUTF8=1` 을 설정할 것.

### 스킬 — 반복 작업은 절차가 정해져 있다

`.claude/skills/` 에 여덟 개가 있다. **해당 작업을 할 때는 반드시 먼저 읽는다.** 매번 다시 추론하면 절차를 빠뜨린다.

**작업용 넷**

| 스킬 | 언제 |
|---|---|
| **`doc-revise`** | 문서를 고칠 때. 스냅샷 → 개정 절 → 공유 수치 대조 → 버전 → 대장 3곳 → `검증.py` |
| **`harmony-verify`** | 렌더·검증할 때. 통과 기준과 기준 음원 실측값이 들어 있다 |
| **`compose-movement`** | 악장을 작곡·수정할 때. 음집합 가드레일과 폐기된 화음 목록 |
| **`review-video`** | 유튜브 검토 영상을 만들 때. showcqt·ASS·인코딩의 함정이 정리돼 있다 |

**하루 관리용 다섯** (v1.12 신설 · v1.13에 `daily-plan` 추가) — 산출물은 `98. 일일 마감/`에 쌓인다.

| 스킬 | 언제 |
|---|---|
| **`daily-plan`** | **"오늘 할 일"이라고 말하면 이것이 처리한다.** 어제 마감 3종과 WBS 선행 관계를 대조해 착수 가능한 것만 고른다 |
| **`daily-closing`** | **"일일마감"이라고 말하면 이것이 전부 처리한다.** 아래 셋을 순서대로 부른다 |
| **`session-log`** | 세션을 마칠 때. 수시. 사실과 숫자만 |
| **`daily-report`** | 하루 1개. 검수자가 읽고 판단할 것만 선별 |
| **`vibe-log`** | 하루 1개. 실수를 **모양끼리 묶는다**. `08` 문서가 이런 기록을 모아 쓴 것이다 |

스킬 이름은 도구가 소문자·하이픈만 허용하므로 R2의 예외다. 스킬은 `백업/` 스냅샷에는 넣지 않는다 — 문서가 아니라 도구이고, git이 이력을 갖는다.

> **스킬은 판정 기준값을 복사하지 않고 정본의 절 번호를 가리킨다** (v1.13 신설 규칙).
> 숫자를 적어야 하면 반드시 출처를 병기한다 — 예: `9:26 F♯ ≥ 0.135 (05 9.10절이 정본)`.
> 복사본이 둘이면 정본이 바뀔 때 반드시 하나는 놓친다. 2026-08-06에 같은 정정을 `harmony-verify`에는 반영하고 `compose-movement`에는 놓쳤고, 그대로 뒀으면 1.4에서 0·2·6·7악장을 정상인데 실패로 읽었을 것이다. 자동 검사는 BL-26.

---

## 6. 확정된 음악 설계 — 바꾸면 안 되는 것

이것들은 여러 문서가 공유하는 값이다. 고칠 일이 생기면 **3절 절차를 밟고** `검증.py`의 `SHARED`도 함께 고친다.

### 음집합 — 이 곡의 중심 아이디어

곡 전체가 **단 하나의 음집합 `B♭ C D E♭ F G A`** 를 쓰고, 악장마다 중심음만 바뀐다.

| 악장 | 중심음 · 선법 |
|---|---|
| 0 · 1 · 2 · 9 | B♭ 이오니안 |
| 3 | G 에올리안 (+ D장3화음의 F♯ = 씨앗) |
| 4 · 7 · 8 | D 프리지안 |
| 6 | **E♭ 리디안** |

**F♯이 이 곡의 유일한 조성 밖 음이다.** 안달루시아 종지 `Gm–F–E♭–D`의 마지막 D 장3화음이 품은 음이고, 그녀(La Duende)의 색채다. 9악장에서 **F♯이 F로 내려앉으며** 주제 B가 B♭장조로 편입된다 — 상실이 아니라 편입.

> A♭·B♮·C♯·E♮를 끌고 들어오는 화음(`G7`·`A7♭9`·`Ddim7`·`A♭maj7` 등)은 **폐기했다.** 이유는 `05` 문서 9.2절에 있다. 다시 넣지 말 것.

### 두 주제 (MusicXML 원본 채보, v1.1 확정)

```python
# 주제 A — 여행자. 9박 프레이즈. 도수 6 5 1 2 5 3 / 2 5 3 1 2
A = [(67,1.0),(65,1.0),(70,1.0),(72,.5),(77,.5),(74,1.0),(72,.5),(77,.5),(74,1.0),(70,1.0),(72,1.0)]
# 주제 B — 그녀. 같은 도수를 D 프리지안에 얹은 것. E♭이 프리지안 ♭2
B = [(70,1.0),(69,1.0),(74,1.0),(75,.5),(81,.5),(77,1.0),(75,.5),(81,.5),(77,1.0),(74,1.0),(75,1.0)]
```

**곡의 엔진은 프레이즈 9박과 마디 11박(5/4↔6/4)의 어긋남이다.** 걸음을 세는 사람과 걷는 사람이 다르다. 두 박자가 맞아떨어지려면 아홉 프레이즈를 가야 한다. 주제를 11박에 맞추면 늘어진다 — 가안이 그렇게 실패했다.

### 성부의 역할

**베이스는 걷는 몸이고, 주제는 보는 눈이다.** 베이스(리켄배커)는 8분음표로 멈추지 않고 걷는 독립 대선율이다. Jon Camp 방식 — 근음을 짚는 악기가 아니라 화성을 의심하는 악기.

**단 4악장 세비야에서 베이스는 걷기를 멈춘다.** 그가 타블라오에 앉아 무희를 보는 순간이고, 12박 컴파스는 그녀의 리듬이지 그의 것이 아니다. 곡 전체에서 베이스가 8분음표 행보를 버리는 유일한 악장이다.

### 악장 타임라인 (초)

`0 · 50 · 90 · 105 · 160 · 260 · 275 · 325 · 425 · 525 · 580`

| 악장 | 구간 | 존재 이유 |
|---|---|---|
| 0 Promenade 제시 | 0:00–0:50 | 주제와 걸음을 기억에 심는다. 베이스가 조성을 선포 |
| 1 마드리드 | 0:50–1:30 | 유일하게 기능화성이 또렷하다. **혼자다 — F♯ 금지** |
| 2 변주 I | 1:30–1:45 | 두 번째 발소리 (기타 캐논) |
| 3 세고비아 | 1:45–2:40 | 코랄. 하강 4음 `G–F–E♭–D` = 4악장 종지의 씨앗 |
| 4 세비야 | 2:40–4:20 | **주제 B 탄생.** 밀도를 무에서 쌓아 올린다. 무그는 마지막 1/5에서만 |
| 5 변주 II | 4:20–4:35 | 두 주제 첫 겹침. **불협을 해소하지 않는다** |
| 6 론다 | 4:35–5:25 | **비움으로 만드는 광활함.** 곡에서 가장 조용하다 |
| 7 그라나다 | 5:25–7:05 | **무그가 그녀의 목소리.** 주제 A는 침묵 |
| 8 바르셀로나 | 7:05–8:45 | 7/8 (2+2+3). 주제 B 파편화. 가장 격렬 |
| 9 The Great Gate | 8:45–9:40 | 플라갈. 두 주제 총주. **9:26 F♯→F 해소** |

### 소재 · 일정 · 비용

유효 사진 **253장**(대부분 1024×768, 43%가 세로 → 멀티패널) · 플라멩코 MOV **9편**(320×240, **무음**, 각 15초) · 총 기간 **30.0 작업일** · 게이트 **G1~G2.5~G7**(8개) · PoC 비용 상한 **$20**, 본편 **$40**.

### 저작권

무소르그스키 원곡 선율은 퍼블릭 도메인. **ELP 1971년 편곡과 호로비츠 편곡은 편곡 저작권이 살아 있어 참조 금지** — 원본 악보(MusicXML)에서 직접 편곡한다. 자체 합성 음원의 권리는 전부 이 프로젝트에 귀속된다(BL-06 판정 근거 중 하나).

### 영상 절대 규칙

| # | 규칙 |
|---|---|
| V1 | 그녀의 **얼굴 클로즈업 금지.** 예외 없음 |
| V2 | **배경 변경 금지** — 건물·하늘·거리는 원본 그대로. 추가되는 것은 그녀 하나, 실루엣으로만 |
| V3 | **4악장 세비야에서는 생성하지 않는다** — 그녀의 유일한 실재는 실제 기록물이어야 한다 |

---

## 7. 현재 위치와 다음 작업

**v1.13 / Phase 1 음악 / G1 승인 완료 · G2 데모 승인 대기.**
**BL-25가 끝나 WBS 1.4의 선행 작업이 모두 해소됐다.** 이제 1.4.1을 막는 것은 G2 승인뿐이다.

산출된 음원·영상:

| 파일 | 무엇 |
|---|---|
| **`전곡 코드 진행 v1.4 - BL-25 베이스 대선율 수작업 9분40초.mp3`** | **현재 코드의 소리 (v1.13, 320kbps)** |
| `전곡 코드 진행 v1.4 - 성부 진행 최적화 9분40초.mp3` | v1.5 납품물. 옛 무그 필터·알고리즘 베이스 |
| `전곡 코드 진행 v1.4 - 화성 검토 영상.mp4` | 유튜브 업로드용. CQT 음정축 + 화음 심볼 263개. **v1.5 음원 기준** |

최근 실측값 (재현 시 이 값과 비교):

| 지표 | 값 |
|---|---|
| 외성 병행 5도 · 8도 | **0건 / 0건** (화음 349개) |
| 평균 총이동량 | 3.93 반음 / 화음전환 (4성부 합) |
| 경직됨 지표 (2~4kHz 포락선 변동계수) | **0.941** — v1.3은 1.185("조잡하다"), 승인받은 건반 스케치는 0.916 |
| 화음 라벨 ↔ 실제 울리는 음 | 88.9% (4악장만 66.2% — 타악 잡음이 크로마에 번지는 측정 한계) |
| 영상↔소리 지연 | 0 샘플 (접합부 포함) |
| 조성 밖 음 합 · 악장별 | 0.086~0.277. **3악장 0.092 · 8악장 0.166** — `05` 9.9절이 정본 |

> **2026-08-06 재실측 주의** — 위 표는 v1.5·v1.6 납품물 기준이다. BL-24b 신규 무그 필터가 들어간 현재 작업본을 재렌더하면 경직됨은 **0.943**, 7악장 RMS는 **−18.2 dB**로 미세하게 움직인다(성부 진행 통계는 349개·3.93반음·병행 0건으로 완전히 동일). **`05` 문서 9.2절 표의 3·8악장 값은 재현되지 않았다** — 경위와 정본 실측표는 9.9절에 있다.
>
> **위 주의는 v1.10에서 해소됐다.** 0.943·−18.2 dB는 BL-24b **작업 중간본**을 잰 값이었다. 새 필터가 무그 스템을 3.8 dB 죽이고 있었고, 그것을 `LEVEL_MATCH`로 되돌린 최종본은 **경직됨 0.941 · 7악장 −18.0 dB**로 v1.5와 완전히 같다. 악장별 RMS 열 개가 전부 일치한다. 상세는 `05` 9.10절.
>
> **v1.13 — 위 표는 이제 기준선이 아니다.** ffmpeg으로 v1.5 납품 mp3를 직접 실측한 결과 **9.2절의 재현 불가는 렌더가 아니라 기록의 오류로 확정**됐다(9악장까지 넷). 그리고 BL-25에서 **전역 난수가 악장을 결합하고 있던 것을 끊었으므로 1~9악장의 흔들림이 한 번 전부 달라졌다.** 현재 값은 경직됨 **0.955** · RMS 폭 **5.8 dB** · 9:26 F♯ **0.136**이고, 성부 진행 다섯 지표(349 · 3.93 · 병행 0/0 · 도약 0 · 밀집 1)는 불변이다. **정본은 `05` 9.11절.**

**승인된 결정 (2026-08-06, v1.8)** — 상세는 `07. 작곡 계획` 5·9장.

1. ✅ **1.4 작곡 순서 변경** — 순차(1→9)에서 **세 기둥(0·4·9) → 도시 → 연결 프롬나드**로. 전환은 양쪽을 알아야 쓸 수 있고, 모든 것이 피날레를 향해야 한다
2. ✅ **G2.5 중간 검수 게이트 신설** — 세 기둥이 선 시점에 한 번 본다. 골격이 틀렸다면 도시 악장 다섯 개를 쓴 뒤보다 훨씬 싸게 고친다. **`01` WBS 1.4.9로 반영, 임계경로 +1.5일(28.5→30.0)**

**착수 대기 (우선순위 순)**

| # | 무엇 | 비고 |
|---|---|---|
| ~~BL-24b~~ | ~~무그 필터 자기발진~~ | **완료 (v1.10)** — 되먹임 라더 `ladder_nl`. 발진 음정 3.9센트, 회귀 없음, 청취 판정 통과. **비명의 악장별 배치는 1.4 소관** |
| ~~BL-25~~ | ~~베이스 대선율 수작업 교체~~ | **완료 (v1.13)** — 0악장(걷기의 원형, 132개 손 작업) · 4악장(밀도 단계별로 앉는 자리) · 6악장(선율을 시간으로 나눠 듦, **`07` 4장 원안과 다르다**). 1·3·7·8악장은 1.4에서 편곡과 함께. 상세는 `05` 9.11절 |
| **WBS 1.4** | 전곡 작곡 — **0악장부터** (첫 기둥) | 선행 작업 전부 해소. **남은 것은 G2 승인뿐이다** |
| BL-27 | 9:26 F♯ 판정 방법 재설계 | 1.4.8 수락 조건이 **자기 측정 흔들림보다 작은 차이**를 재고 있다(씨앗만 바꿔도 0.119~0.136) |
| BL-26 | 스킬 문서의 수치 사본 정합 검사 | `검증.py` 7번째 검사. 5절 규칙을 자동으로 지키게 한다 |
| BL-23 | 현악 비중 | 6악장 재설계로 요구가 줄었다. 현재 피아노 대비 −18 dB |
| BL-05 · BL-21 | 보칼리즈 | 7악장은 무그로 확정. 사람 목소리를 쓸지는 미결 |
| BL-22 | 마드리드 13장에 "빈 의자" 사진이 실제로 있는지 확인 | 마지막 컷의 전제 |

---

## 8. 작업 방식

- **긴 렌더는 시간을 알려주고 시작한다.** 전곡 렌더는 2코어에서 4~5분이었다
- **음악은 귀가 아니라 숫자로 먼저 검증한다.** 크로마·온셋·대역·성부 이동량을 실측하고 문서에 값을 남긴다. "좋아졌다"가 아니라 "1.185 → 0.941"로 말한다
- **제약을 컨셉으로 바꾼다.** 이 프로젝트의 좋은 결정은 대부분 그렇게 나왔다 — 저해상도 → "상상 속 인물에게는 얼굴이 없다", 약한 현악 → "비움으로 만드는 광활함", 사람 목소리 부재 → "언어 없는 목소리에는 신디사이저가 맞다", 음집합 보존 → "E♭ 리디안"
- **틀렸으면 인정하고 원문을 남긴다.** 개정 절에 무엇이 왜 바뀌었는지 적는다(R3)
- 검수자는 **프로그레시브 록**을 좋아한다. 기준은 **Renaissance 〈Mother Russia〉**(주), ELP 《전람회의 그림》(변주 원천). 1970년대 신디사이저 소리를 좋아하지만 **그것이 조잡한 소리를 받아들인다는 뜻은 아니다**

---

## 9. 하지 말 것

- 문서 내용 삭제 (R3)
- 파일명에 언더바 (R2)
- 음집합 밖의 음을 끌고 오는 화음 추가 (6절)
- **1악장에 F♯** — 그 악장의 서사는 "혼자"다
- 4악장에 생성 영상 (V3), 그녀의 얼굴 클로즈업 (V1), 배경 변경 (V2)
- ELP·호로비츠 편곡 참조 (6절)
- `sketch.py`·`demo60.py`·`themes_v12.py`·`themes_v13.py` 수정 — 과거 버전 기록이다
- **G3 PoC 통과 전에 사진으로 영상 만들기** — Phase 3 작업을 앞당기면 버리는 일이 된다
- 문서를 고치고 `검증.py`를 안 돌리기 (R5)

---

*최종 갱신 2026-08-07 · 프로젝트 v1.13*
