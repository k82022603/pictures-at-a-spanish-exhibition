# CLAUDE.md

《스페인 전람회의 그림》 — 2005년 12월 스페인 여행 기록으로 만드는 **9분 40초 심포닉 프로그레시브 뮤직비디오**. 음악은 라이선스가 아니라 **직접 작곡·합성**한다.

이 파일은 Claude Code가 세션 시작 시 자동으로 읽는다. 대화 맥락은 세션 사이에 이어지지 않으므로, **이어서 작업하려면 이 파일과 `00`·`01`·`07` 문서를 먼저 읽어야 한다.**

---

## 1. 절대 규칙

검수자(JinYong)가 명시한 규칙이다. 어기면 다시 해야 한다.

| # | 규칙 | 이유 |
|---|---|---|
| R1 | **문서는 마크다운이 기본.** docx·pdf는 파생물이며 `백업/` 아래 해당 버전 폴더에 둔다 | — |
| R2 | **파일명에 언더바 금지.** 공백과 구체적인 단어로. 문서는 `00. `·`01. ` 일련번호 접두사 | 정렬 순서가 곧 작업 순서 |
| R3 | **기존 문서 내용을 절대 삭제하지 않는다.** 판단이 바뀌면 원문을 남기고 "개정" 절을 덧붙여 **무엇이 왜** 바뀌었는지 적는다 | 변경 이력 자체가 나중에 판단의 근거가 된다 |
| R4 | **답변은 한글.** 모든 답변 끝에 서버 날짜를 확인해 `작성 일자: YYYY-MM-DD` 로 적는다 | — |
| R5 | 문서를 고쳤으면 **반드시** `python3 "99. 작업 스크립트/검증.py"` 를 돌린다 | 6번 검사가 스냅샷 누락을 자동으로 잡는다 |
| R6 | 계획·설계 문서는 **서술형**을 선호한다. 표만 나열하지 말고 왜 그렇게 하는지 쓴다 | `07. 작곡 계획`이 그 예 |
| **R7** | **검수자는 음향·미디 전공이 아니다.** 전문 용어를 쓰면 **그 자리에서 풀어 쓴다.** 음원을 들려줄 때는 반드시 **「무엇이 다른가 · 왜 그런가 · 어디를 들을 것인가」 표**를 붙인다. 8절에 형식이 있다 | 용어가 막히면 판정 자체가 막힌다 |
| **R8** | **하루를 닫는 일은 검수자가 직접 말했을 때만 한다.** `daily-closing`을 계획서에 적어두고 스스로 발동하지 않는다. **계획 승인은 절차의 승인이지 "지금 하루를 닫아도 된다"는 뜻이 아니다** | 하루가 끝났는지는 검수자가 안다 |

`CLAUDE.md`와 `.gitignore`는 도구가 이름을 강제하므로 R2의 예외다.

파일명에 공백이 있으므로 마크다운 링크는 `[제목]` 뒤에 괄호와 꺾쇠(`<` `>`)로 파일명을 감싸는 형태로 쓴다 — 예: 대괄호에 제목, 소괄호 안 꺾쇠에 `00. 스페인 전람회의 그림 기획서.md`.

---

## 2. 문서 체계 — 무엇이 어디 있는가

**이 표는 목차일 뿐이다. 내용을 여기에 복사하지 말 것.** 같은 사실이 두 곳에 있으면 반드시 어긋난다.

| 문서 | 무엇이 들어 있나 | 상태 |
|---|---|---|
| `00. 스페인 전람회의 그림 기획서.md` | 최상위 기획. 악장 구성표, 편성, 영상 설계(V1~V3 절대 규칙), 리스크, 게이트 G1~G7 | **G1 승인** |
| `01. 작업 분해 구조와 백로그.md` | WBS 51개 + 백로그 **BL-01~BL-32**, 임계경로 30.0 작업일. **1.4.1 완료** | 진행 |
| `02. AI 영상 생성 기술 검증 계획서.md` | G3 PoC 스파이크. 가설 H1~H7, 테스트 T1~T8 | 착수 전 |
| `03. 스토리라인과 등장인물.md` | 서사. 그녀(La Duende) 설정, 악장별 서사, 두 주제 설계. **8장에 노랫말 개정 절** | **G1 승인** |
| `04. 문서 개정 이력.md` | 버전 대장 **v0.1→v3.4**, 운영 절차, 되돌리기 | 상시 갱신 |
| `05. 코드 진행 스케치 검토서.md` | 악장별 화성. **9장 = 전곡 화성 v1.4, 10장 = 유튜브 검토 영상**. 9.9~9.19절이 실측 정본 (**9.13 = 0악장 · 9.14 = RMS 폭 기준 · 9.15 = 4악장 · 9.16 = 9악장**) | **★ G2 승인** |
| `06. 음악 제작 방식 결정서.md` | BL-06 판정 — 자체 합성 유지. Suno와의 차이, 제약, 재검토 조건 | 결정 완료 |
| `07. 작곡 계획.md` | WBS 1.4 전곡 작곡 설계. **서술형**. 10장 베이스 대선율 · **11장 노랫말** · **12장 0악장** · **13장 4악장의 공간** · **14장 9악장** · **15장 드럼 편성** · **16장 드럼 패턴** · **17장 「지금 채운다」** · **18장 8악장 인스트루멘털 브레이크** | **★ G2 승인 — 1.4 착수 가능** |
| `08. 중간 점검과 인수인계.md` | Phase 1 회고. 판단이 바뀐 다섯 번, 남은 위험, 실무 팁. **9장 = G2.5 직전 자체 평가 — 곡의 65%가 아직 스케치다** | 기록 |
| 09 · 10 (예정) | 프롬프트 규격서(G3 통과 후) · 스토리보드(Phase 3) | 미작성 |

`자료/` = 참고 악보·MusicXML. `2005년 12월 스페인/` = 원본 사진 253장 + 무음 MOV 9편.

### `96. 운영/` — 저장소와 산출물 (v2.5 신설)

**도구 사용법이다.** 문서·코드는 **GitHub**이, 음원·영상은 **Google Drive**가 갖는다. 그 경계와 명령이 [`96. 운영/00. 저장소와 산출물 관리 가이드`](<96. 운영/00. 저장소와 산출물 관리 가이드.md>)에 있다.

| | 어디에 |
|---|---|
| 저장소 | <https://github.com/k82022603/pictures-at-a-spanish-exhibition> (**공개**) |
| 산출물 | [Drive — MusicVideo](https://drive.google.com/drive/folders/1VKkKWI5kTLqeCjlnBL5GTZsc6JbDQHmu) |

**`산출물/YYYYMMDD - 한 마디/README.md`가 둘을 잇는다** — README는 git에 있고 파일은 Drive에 있다. `98. 일일 마감/`과 같은 R2 예외이며 근거도 같다(날짜 정렬 = 시간 순서).

> **음원·영상을 커밋하지 않는다.** git은 한 번 넣은 파일을 영원히 기억하고, 검토 영상 한 편(114.6 MB)은 GitHub의 100 MB 한도를 넘는다. `.gitignore`가 막고 있으나 새 확장자를 쓸 때는 가이드 3절의 확인을 돌린다.

**렌더가 비트 단위로 재현된다는 것(BL-30)은 코드가 남아 있을 때만 자산이다.** 2026-08-08 이전에는 git이 없었고 `백업/`은 마크다운만 담았으므로 **G2 승인 음원을 다시 만들 수 없다.** 그 이전 산출물은 Drive 보관이 유일한 보존이고, 각 날짜 README 머리에 그렇게 적혀 있다.

97·98과 같이 **프로젝트 버전 체계 밖**이다 — 도구는 음악 설계와 다른 속도로 바뀐다.

### `97. 회고/` — 트러블슈팅과 용어 (v1.14 신설)

**사건 단위 기록이다.** 무엇이 문제였고, **왜 그때는 몰랐고**, 어떻게 고쳤는지. 98의 바이브로그가 하루 단위로 판단을 되짚는다면, 여기는 같은 종류의 문제를 다시 만났을 때 **찾아오는 곳**이다.

| 파일 | 무엇 |
|---|---|
| `00. 용어 사전.md` | **렌더·스템·RMS·크로마·경직됨 지표·성부·게이트 등.** 보고서를 읽다 막히면 여기부터 |
| `01. 트러블슈팅 색인.md` | 폴더 규칙과 색인. 사건들을 관통하는 것 |
| `T-NN. <제목>.md` | 사건 하나에 문서 하나. 현재 **T-01~T-08** |

**각 사건 문서에는 "어제는 왜 못 봤나" 절이 반드시 들어간다** — 원인은 한 번 고치면 끝이지만 **인지 실패의 구조는 남기** 때문이다. "부주의했다"로 끝내지 않는다. 고치지 못하고 절차로만 막은 것도 적는다.

98과 같이 **프로젝트 버전 체계 밖**이다 — 버전 없음, `백업/` 스냅샷에 안 넣음, `검증.py`가 안 봄.

### `98. 일일 마감/` — 작업 일지 (v1.12 신설 · v1.13 계획 추가)

위 표의 문서들과 **성격이 다르다.** 명세가 아니라 일지이므로 **프로젝트 버전 체계 밖**이다.

| 파일 | 주기 | 무엇 |
|---|---|---|
| **`YYYYMMDD-0-daily-plan.md`** | 하루 1개 · 아침 | **오늘 할 일과 왜 오늘인지.** 하지 않을 것까지 |
| `YYYYMMDD-HHMM-session-log.md` | 수시 | 사실과 숫자. **아래 둘의 원본** |
| `YYYYMMDD-daily-report.md` | 하루 1개 · 저녁 | 검수자가 읽고 판단할 것 |
| `YYYYMMDD-vibe-log.md` | 하루 1개 · 저녁 | 숫자로 안 남는 것 — 판단의 경로, 실수의 모양 |
| **`YYYYMMDD-HHMM-expectation-<주제>.md`** | 렌더 전 | **손대기 전에 무엇이 변해야 하는가.** git 이 "그때 적었다"를 증명한다 |

파일명은 **영어 + `YYYYMMDD-` 접두사**다(R2의 예외 — 날짜 정렬이 곧 시간 순서다). 계획만 `-0-`을 끼우는데, 하이픈(0x2D)이 어떤 숫자보다 앞서므로 **이름순 정렬이 곧 하루의 순서**가 된다. 버전을 매기지 않고 `백업/` 스냅샷에도 넣지 않으며, `검증.py`는 루트 `*.md`만 훑으므로 이 폴더를 보지 않는다.

**"오늘 할 일"이라고 말하면 `daily-plan`이, "일일마감"이라고 말하면 `daily-closing`이 전부 처리한다.**

계획에는 규칙이 셋 있다. **① 명세를 복사하지 않고 절 번호로 가리킨다** — 2절의 경고가 일일 문서에도 적용된다. **② 틀려도 고치지 않는다** — 저녁에 결과에 맞춰 손보면 그 순간 회고 재료가 사라진다. 어긋난 자리는 데일리보고 3절과 바이브로그가 다룬다(R3를 하루 단위로 적용한 것이다). **③ 하지 않기로 한 것도 적는다** — 없으면 다음날 같은 것을 또 후보로 올린다.

---

## 3. 수정 절차

문서를 고칠 때는 이 순서를 지킨다. `04. 문서 개정 이력` 5장에 있는 절차와 같다.

```
1. 수정 전  →  백업/vN.N (날짜) 요약/  에 전체 .md 복사
2. 수정      →  기존 내용은 지우지 않고 "개정" 절을 덧붙인다 (R3)
3. 대조      →  바뀐 수치가 다른 문서에도 있으면 함께 고친다
4. 버전      →  각 문서 머리 표의 버전을 올리고 개정 이력 행 추가
5. 대장      →  04 문서 3장 전체 이력에 행 추가
6. 검증      →  python3 "99. 작업 스크립트/검증.py"
```

**버전 규칙** — `v[MAJOR].[MINOR]`. 프로젝트 전체에 버전 하나를 붙인다(문서별 버전이 아니다). MAJOR는 검수 게이트 통과 시(G1 승인 → v1.0), MINOR는 내용 변경 시. 오탈자·서식만 고쳤으면 올리지 않고 이력에 `(서식)`으로 남긴다. 되돌린 경우도 앞으로 흐르는 새 버전으로 기록한다.

문서별 버전을 쓰지 않는 이유는 문서들이 **같은 수치를 공유**하기 때문이다(사진 253장, 총 기간 30.0일 등). 개별 버전이면 "기획서 v1.2 + WBS v2.1"이 맞는 조합인지 매번 따져야 한다.

**현재 버전: v3.4** (2026-08-08). 최신 스냅샷 `백업/v3.4 (2026-08-08) 인스트루멘털 브레이크/`.

> **이 줄은 `검증.py` 5번 검사에서 제외돼 있다.** 버전을 올릴 때 손으로 고쳐야 하고, 실제로 v1.14에서 v2.1까지 일곱 번 밀려 있었다. **고칠 때 이 절과 파일 끝줄, 그리고 2절 표의 `01`·`04` 행까지 넷을 본다.**

MINOR가 두 자리로 넘어갔다. **`백업/` 폴더를 이름순으로 정렬하면 `v1.10`이 `v1.2`보다 앞에 온다** — 사람이 눈으로 훑을 때도, 스크립트가 최신을 고를 때도 함정이다. `검증.py`는 v1.10에서 버전 번호를 숫자로 파싱하도록 고쳤다.

---

## 4. 실행 환경

### 필요한 것

| 항목 | 비고 |
|---|---|
| Python 3 | `numpy` · `scipy` · `Pillow` |
| **ffmpeg / ffprobe** | **2026-08-07 설치 완료** — Gyan.FFmpeg 9.0 (`winget install --id Gyan.FFmpeg`). 사용자 PATH에 등록되므로 **새 셸에서만 잡힌다.** 렌더·인코딩·MP3 변환에 쓴다. 설치 직후 `05` 9.9절이 미확인으로 남겼던 v1.5 납품 mp3 실측을 마쳤고, 결론이 9.11절에 있다. ~~2026-08-06 확인 — 현재 PC에 설치돼 있지 않다~~ |
| **`gh` CLI** | **2026-08-08 설치** — GitHub CLI 2.97.0 (`winget install --id GitHub.cli`). 저장소 생성·푸시. **인증은 `gh auth login`으로 검수자가 한 번** 한다 |
| **`rclone`** | **2026-08-08 설치** — 1.75.0 (`winget install --id Rclone.Rclone`). 산출물을 Drive로 올린다. **인증은 `rclone config reconnect drive:`로 검수자가 한 번** 한다. 설정은 `%AppData%/rclone/rclone.conf`, 원격 최상위가 프로젝트 Drive 폴더다 |
| **Git for Windows** | Windows에서 Claude Code가 Bash 도구를 쓰려면 필요하다. 없으면 PowerShell로 대체되는데, `.claude/skills/` 의 명령 예시가 전부 POSIX 셸 기준이라 그대로는 동작하지 않는다 |

첫 세션에서 아래 셋을 먼저 확인할 것.

```bash
python -c "import numpy, scipy, PIL; print('ok')"
ffmpeg -version
bash --version        # POSIX 셸이 잡히는지
gh auth status        # GitHub 인증이 살아 있는지
rclone listremotes    # drive: 가 나와야 한다
```

> **winget으로 깐 도구는 새 셸에서만 PATH에 잡힌다.** ffmpeg·gh·rclone 셋 다 그렇다.

### 주요 명령

`99. 작업 스크립트/` 안에서 실행한다.

```
python 전곡화성.py      # 전곡 9분40초 렌더 → 전곡화성.wav (+ chordlog.npy + 스템/)
python 믹스.py          # 스템으로 믹스만 다시 (20초). 페이더·성부별 잔향 실험용
python 검증화성.py      # 크로마·대역·경직됨·악장별 RMS 실측
python 자막생성.py      # chordlog.npy → 자막.ass
python 음정축.py        # CQT 음정축 이미지 → 음정축.png
python 검증.py          # 문서 정합성 6종 검사  (상위 폴더에서 실행)
```

영상 렌더는 `영상필터.txt`의 필터그래프를 개행 제거해 `-filter_complex_script`로 넘긴다. 10장에 규격이 있다.

### 성능 주의

이전 작업은 2코어·메모리 3.9GB 샌드박스에서 했다. 그 제약 때문에 두 가지가 코드에 남아 있다.

- ~~**스템을 따로 보관하지 않는다**~~ → **v2.1에서 되돌렸다 (BL-29).** `Desk(stems=True)`가 기본이고 성부별 스테레오 버퍼를 든다(1.35 GB, 이 PC 15.7 GB). 페이더는 **믹스 시점**에 곱하고, 성부마다 다른 잔향을 줄 수 있으며, 스템을 디스크에 내려두면 **렌더 없이 믹스만** 다시 할 수 있다. 옛 단일 버퍼 경로는 `stems=False`로 남아 있다 — 지우지 않았다
- **캐시에 상한을 두고 세기·길이를 양자화한다** — 연주 흔들림이 세기를 연속적으로 흔들면 캐시 키가 매번 달라져 메모리가 터진다. `화성.py`의 `pn`·`st`·`ny`·`rick`·`hamm`·`moog`가 그 래퍼다
- **씨앗 없는 난수를 쓰지 않는다** (v2.1, BL-30). `np.random.*`(전역)은 프로세스마다 다르게 초기화되어 **렌더할 때마다 다른 소리가 난다.** 반드시 `np.random.default_rng(씨앗)`을 쓴다. 파이썬 `hash()`도 씨앗으로 쓰지 않는다 — 실행마다 다르다. `synth.py`에 아직 여섯 곳(드럼·`bass()`)이 남아 있고 **1.1.6 드럼 재설계에서 살아난다**(BL-31)

**렌더는 비트 단위로 재현된다** (v2.1 이후). 같은 코드를 두 번 돌리면 100.0000% 같은 wav가 나온다. 그 전에는 아니었고, 지표 해상도 아래에 있어서 넉 달 동안 몰랐다 — `97. 회고` T-06.

---

## 5. 코드 지도

| 파일 | 역할 |
|---|---|
| `piano.py` | 피아노. 가산합성 + 현 비조화도 + 이중 감쇠. `note()` · `Score` · `_room()` |
| `ensemble.py` | 현악(8성부 디튠) · 나일론기타(Karplus-Strong) · 팔마스 · 카혼 · 타콘 · **리켄배커 베이스** `rick()` |
| `synth.py` | 해먼드(9드로바 + 레슬리) · 무그 · 베이스 · 드럼 · `Mix`. **무그는 되먹임 라더 `ladder_nl`** — 자기발진한다(BL-24b). 옛 `ladder_sweep`은 되먹임이 없어 발진 불가이며 `bass()`만 아직 쓴다 |
| **`화성.py`** | **화성 엔진.** 화음 사전, `voice_lead()` 성부 진행 최적화, `bassline()` 베이스 생성, `tape()` 테이프 새추레이션, `room()`·**`plate()`** 잔향, **`Desk` 콘솔 — 스템별 버퍼·성부별 센드·스템 입출력** |
| **`믹스.py`** | **스템으로 믹스만 다시 한다** (v2.1). 렌더 209초 → 믹스 20초. `--gain org=-11 --send lead=plate:0.42` 꼴로 실험한다 |
| **`전곡화성.py`** | 전곡 10악장 악보. 악장별 진행·편성·텍스처 |
| `검증화성.py` | 음원 검증 — 크로마, 조성 밖 음, 대역 균형, 경직됨 지표, 악장별 RMS, F♯→F |
| `검증무그.py` · `데모무그.py` | BL-24b — 자기발진 여부·발진 음정·진폭 안정성 검사 / 옛 필터·새 필터·비명 3구간 청취 데모 |
| **`악장대조.py`** | **두 wav 를 악장별로 대조.** T-07 규칙 — 고친 악장만 보지 않는다. `python 악장대조.py 기준선.wav 전곡화성.wav` |
| **`보이싱탐색.py`** | 보이싱 범위를 **렌더 없이** 고른다. `05` 9.13.2절의 25조합 표가 이것으로 나왔다 |
| `자막생성.py` · `음정축.py` | 유튜브 검토 영상의 자막(ASS)과 음정축 이미지 |
| **`악장표.py`** | **악장별 사실을 코드와 음원에서 생성.** 손으로 안 적는다. 빠르기를 ♪/♩ 로 구분해 채보 실수를 막고, 숫자와 **풀어쓴 판**을 함께 찍는다 |
| `검증.py` | 문서 정합성. 표·링크·앵커·공유 수치·버전·**스냅샷 누락** |
| `extract.py` · `채보.py` | MusicXML 추출 / 악보 이미지 OMR (주제 확정에 사용, 완료) |
| `sketch.py` · `demo60.py` · `themes_v12.py` · `themes_v13.py` | 과거 버전 데모. 참고용이며 수정하지 않는다 |

한글 모듈명(`화성.py`)을 `import 화성` 으로 쓴다. Windows에서 인코딩 문제가 생기면 `PYTHONUTF8=1` 을 설정할 것.

### 스킬 — 반복 작업은 절차가 정해져 있다

`.claude/skills/` 에 **열한 개**가 있다. ~~여덟 개~~ — v1.13에서 `daily-plan`을 더할 때 이 숫자를 안 고쳐 하나가 밀려 있었다(v2.4에서 정정). **해당 작업을 할 때는 반드시 먼저 읽는다.** 매번 다시 추론하면 절차를 빠뜨린다.

**작업용 넷**

| 스킬 | 언제 |
|---|---|
| **`doc-revise`** | 문서를 고칠 때. 스냅샷 → 개정 절 → 공유 수치 대조 → 버전 → 대장 3곳 → `검증.py` |
| **`harmony-verify`** | 렌더·검증할 때. 통과 기준과 기준 음원 실측값이 들어 있다 |
| **`compose-movement`** | 악장을 작곡·수정할 때. 음집합 가드레일과 폐기된 화음 목록 |
| **`review-video`** | 유튜브 검토 영상을 만들 때. showcqt·ASS·인코딩의 함정이 정리돼 있다 |

**하루 관리용 여섯** (v1.12 신설 · v1.13에 `daily-plan` · v2.4에 `compose-log` 추가) — 산출물은 `98. 일일 마감/`에 쌓인다.

| 스킬 | 언제 |
|---|---|
| **`daily-plan`** | **"오늘 할 일"이라고 말하면 이것이 처리한다.** 어제 마감 3종과 WBS 선행 관계를 대조해 착수 가능한 것만 고른다 |
| **`daily-closing`** | **"일일마감"이라고 말하면 이것이 전부 처리한다.** 아래 넷을 부른 뒤 **산출물 README 정리 · Drive 업로드 · GitHub 푸시**까지 간다 |
| **`session-log`** | 세션을 마칠 때. 수시. 사실과 숫자만 |
| **`daily-report`** | 하루 1개. 검수자가 읽고 판단할 것만 선별 |
| **`vibe-log`** | 하루 1개. 실수를 **모양끼리 묶는다**. `08` 문서가 이런 기록을 모아 쓴 것이다 |
| **`minimal-fill`** | **예정에 없던 구멍을 지금 채울지 미룰지.** 순서를 어기는 판단이라 근거를 남긴다. `07` 17장이 정본 |
| **`compose-log`** | 하루 1개. **곡에 대한 기록** — 무엇을 버렸나, 왜 그 값인가. 바이브로그의 주어가 「나」라면 이쪽은 「곡」이다 |

스킬 이름은 도구가 소문자·하이픈만 허용하므로 R2의 예외다. 스킬은 `백업/` 스냅샷에는 넣지 않는다 — 문서가 아니라 도구이기 때문이다. ~~git이 이력을 갖는다~~ — **2026-08-08 확인 결과 이 프로젝트는 git 저장소가 아니었다.** 즉 스킬도 `99. 작업 스크립트/`의 코드도 이력이 아무 데도 없다. GitHub 도입이 이것을 푼다(오늘 마감 예정).

> **스킬은 판정 기준값을 복사하지 않고 정본의 절 번호를 가리킨다** (v1.13 신설 규칙).
> 숫자를 적어야 하면 반드시 출처를 병기한다 — 예: `9:26 F♯ ≥ 0.135 (05 9.10절이 정본)`.
> 복사본이 둘이면 정본이 바뀔 때 반드시 하나는 놓친다. 2026-08-06에 같은 정정을 `harmony-verify`에는 반영하고 `compose-movement`에는 놓쳤고, 그대로 뒀으면 1.4에서 0·2·6·7악장을 정상인데 실패로 읽었을 것이다. 자동 검사는 BL-26.

---

## 6. 확정된 음악 설계 — 바꾸면 안 되는 것

이것들은 여러 문서가 공유하는 값이다. 고칠 일이 생기면 **3절 절차를 밟고** `검증.py`의 `SHARED`도 함께 고친다.

### 음집합 — 이 곡의 중심 아이디어

곡 전체가 **단 하나의 음집합 `B♭ C D E♭ F G A`** 를 쓰고, 악장마다 중심음만 바뀐다.

| 악장 | 중심음 · 선법 |
|---|---|
| 0 · 1 · 2 · 9 | B♭ 이오니안 |
| 3 | G 에올리안 (+ D장3화음의 F♯ = 씨앗) |
| 4 · 7 · 8 | D 프리지안 |
| 6 | **E♭ 리디안** |

**F♯이 이 곡의 유일한 조성 밖 음이다.** 안달루시아 종지 `Gm–F–E♭–D`의 마지막 D 장3화음이 품은 음이고, 그녀(La Duende)의 색채다. 9악장에서 **F♯이 F로 내려앉으며** 주제 B가 B♭장조로 편입된다 — 상실이 아니라 편입.

> A♭·B♮·C♯·E♮를 끌고 들어오는 화음(`G7`·`A7♭9`·`Ddim7`·`A♭maj7` 등)은 **폐기했다.** 이유는 `05` 문서 9.2절에 있다. 다시 넣지 말 것.

### 두 주제 (MusicXML 원본 채보, v1.1 확정)

```python
# 주제 A — 여행자. 9박 프레이즈. 도수 6 5 1 2 5 3 / 2 5 3 1 2
A = [(67,1.0),(65,1.0),(70,1.0),(72,.5),(77,.5),(74,1.0),(72,.5),(77,.5),(74,1.0),(70,1.0),(72,1.0)]
# 주제 B — 그녀. 같은 도수를 D 프리지안에 얹은 것. E♭이 프리지안 ♭2
B = [(70,1.0),(69,1.0),(74,1.0),(75,.5),(81,.5),(77,1.0),(75,.5),(81,.5),(77,1.0),(74,1.0),(75,1.0)]
```

**곡의 엔진은 프레이즈 9박과 마디 11박(5/4↔6/4)의 어긋남이다.** 걸음을 세는 사람과 걷는 사람이 다르다. 두 박자가 맞아떨어지려면 아홉 프레이즈를 가야 한다. 주제를 11박에 맞추면 늘어진다 — 가안이 그렇게 실패했다.

### 성부의 역할

**베이스는 걷는 몸이고, 주제는 보는 눈이다.** 베이스(리켄배커)는 8분음표로 멈추지 않고 걷는 독립 대선율이다. Jon Camp 방식 — 근음을 짚는 악기가 아니라 화성을 의심하는 악기.

**단 4악장 세비야에서 베이스는 걷기를 멈춘다.** 그가 타블라오에 앉아 무희를 보는 순간이고, 12박 컴파스는 그녀의 리듬이지 그의 것이 아니다. 곡 전체에서 베이스가 8분음표 행보를 버리는 유일한 악장이다.

### 악장 타임라인 (초)

`0 · 50 · 90 · 105 · 160 · 260 · 275 · 325 · 425 · 525 · 580`

> **악장이 흐르는 순서는 0→9이고 한 번도 바뀐 적이 없다.** 위 타임라인이 정본이고, `00` 기획서 악장 구성표 · `검증화성.py`의 `MOV` · `자막생성.py`의 `MOV` · `전곡화성.py`의 `mark()` 네 곳이 모두 이것과 같다.
>
> **v1.8에서 바뀐 것은 "작곡하는 순서"다** — 순차(1→9)에서 세 기둥(0·4·9) → 도시 → 연결 프롬나드로. 그런데 `01` WBS 1.4 표가 그 작업 순서대로 행을 재배열해 놓아서 `1.4.1 → 1.4.4 → 1.4.8 → 1.4.2 …`로 보인다. **곡의 순서가 아니라 내 작업 순서다.** 헷갈리기 쉬우므로 표를 읽을 때 주의한다.

| 악장 | 구간 | 존재 이유 |
|---|---|---|
| 0 Promenade 제시 | 0:00–0:50 | 주제와 걸음을 기억에 심는다. 베이스가 조성을 선포 |
| 1 마드리드 | 0:50–1:30 | 유일하게 기능화성이 또렷하다. **혼자다 — F♯ 금지** |
| 2 변주 I | 1:30–1:45 | 두 번째 발소리 (기타 캐논) |
| 3 세고비아 | 1:45–2:40 | 코랄. 하강 4음 `G–F–E♭–D` = 4악장 종지의 씨앗 |
| 4 세비야 | 2:40–4:20 | **주제 B 탄생.** 밀도를 무에서 쌓아 올린다. 무그는 마지막 1/5에서만 |
| 5 변주 II | 4:20–4:35 | 두 주제 첫 겹침. **불협을 해소하지 않는다** |
| 6 론다 | 4:35–5:25 | **비움으로 만드는 광활함.** 곡에서 가장 조용하다 |
| 7 그라나다 | 5:25–7:05 | **무그가 그녀의 목소리.** 주제 A는 침묵 |
| 8 바르셀로나 | 7:05–8:45 | 7/8 (2+2+3). 주제 B 파편화. 가장 격렬. **이 곡의 유일한 연주 구간** — 4마디마다 주역이 바뀌고 비명이 여기 있다 (`07` 18장) |
| 9 The Great Gate | 8:45–9:40 | 플라갈. 두 주제 총주. **9:26 F♯→F 해소** |

### 소재 · 일정 · 비용

유효 사진 **253장**(대부분 1024×768, 43%가 세로 → 멀티패널) · 플라멩코 MOV **9편**(320×240, **무음**, 각 15초) · 총 기간 **30.0 작업일** · 게이트 **G1~G2.5~G7**(8개) · PoC 비용 상한 **$20**, 본편 **$40**.

### 노랫말 — 여행자가 부른다 (v1.15)

**주제 A(여행자)에 노랫말을 얹는다. 주제 B(그녀)는 끝까지 말하지 않는다** — 7악장 "무그 = 언어 없는 목소리" 설계는 그대로다.

| 항목 | 결정 |
|---|---|
| 부르는 이 | **여행자 (주제 A)** |
| 놓이는 자리 | **프롬나드 네 악장 0·2·5·9에만.** 도시 악장 여섯은 그가 보는 것이지 말하는 것이 아니다 |
| 언어 | **영어로 확정** (v1.16). 스페인어 아님 |
| 텍스트 | 로르카를 **어휘와 태도의 출처로만** 삼고 직접 쓴다 (인용·번역 아님) |
| 목소리 | **외부 도구.** BL-28 PoC로 먼저 검증 |
| 목소리 처리 | 레이드백 타이밍 · 지속음 크레셴도 · 부드러운 어택 · 로우미드 온기 · 플레이트 잔향. **목소리가 어디서 왔든 적용된다.** 무그(그녀)에도 같이 적용해 두 목소리가 같은 공기를 갖게 한다 |
| 음역 | 주제 A 원음 **MIDI 65~77 (F4~F5) · 349~698 Hz.** 옥타브를 내리지 않는다 — 내리면 화성 보이싱(50~70)에 묻힌다 |
| 고음 부담 | 11음 중 **4개가 C5를 넘는다**(D5 둘·F5 둘). 다만 **두 F5는 8분음표(0.3초)**라 스쳐 지나가므로 지속 최고음은 D5다. **벨팅 라인이 아니라 고음이 스치는 라인.** BL-28은 이 네 음을 본다 |
| 음절 | 프레이즈당 **11음절.** 4·5번째와 7·8번째가 8분음표라 짧게 붙는 음절 |

> **1.4 작업 규칙 — 프롬나드 네 악장의 화성 보이싱 상한을 63~65로 낮춘다.**
> 현재 0악장 50~70 · 2악장 50~68 · 5악장 51~70 · 9악장 52~72라 목소리(65~77)와 겹친다. 이것이 이 결정의 **유일한 즉시 반영 사항**이다. 나머지는 나중에 정해도 1.4를 막지 않는다.
> **2악장은 주제 A를 나일론 기타가 원음역으로 연주하므로 별도 처리가 필요하다.**

상세는 `07. 작곡 계획` 11장, 서사 근거는 `03` 8장 개정 절.

### 저작권

무소르그스키 원곡 선율은 퍼블릭 도메인. **ELP 1971년 편곡과 호로비츠 편곡은 편곡 저작권이 살아 있어 참조 금지** — 원본 악보(MusicXML)에서 직접 편곡한다. 자체 합성 음원의 권리는 전부 이 프로젝트에 귀속된다(BL-06 판정 근거 중 하나).

**ELP 앨범의 가사도 마찬가지로 쓰지 않는다** (v1.15). 무소르그스키와 무관한 창작이고 저작권이 살아 있다. 편곡을 참조하지 않기로 한 이유가 가사에는 더 강하게 적용된다.

**선은 여기다** (v1.18) — **창법·음역·음색은 참조해도 된다.** 사실이고 저작 대상이 아니다. **편곡·가사·구성은 금지다.** "그렇게 부르는 소리를 목표로 삼는다"는 생산 목표이고, "그 편곡을 따라 쓴다"는 규칙 위반이다.

**번역은 별개의 저작물이다.** 원문이 퍼블릭 도메인이어도 시중 번역본에는 번역자의 저작권이 살아 있다. 로르카 원문은 한국에서 1986년에 풀렸지만, **기성 한국어·영어 번역본은 가져다 쓸 수 없다.** 그래서 인용하지 않고 직접 쓴다.

**외부 도구로 만든 보컬은 약관을 확인한다** (BL-28). 상업적 이용·크레딧 의무·재현성이 도구마다 다르다. **생성물은 반드시 보관한다** — 다시 못 뽑으면 믹스를 다시 짤 수 없고, 그것은 BL-06이 자체 합성을 택한 근거(재현성)를 건드린다.

### 영상 절대 규칙

| # | 규칙 |
|---|---|
| V1 | 그녀의 **얼굴 클로즈업 금지.** 예외 없음 |
| V2 | **배경 변경 금지** — 건물·하늘·거리는 원본 그대로. 추가되는 것은 그녀 하나, 실루엣으로만 |
| V3 | **4악장 세비야에서는 생성하지 않는다** — 그녀의 유일한 실재는 실제 기록물이어야 한다 |

---

## 7. 현재 위치와 다음 작업

**v2.4 / Phase 1 음악 / G1 · G2 승인 완료.**
**★ 세 기둥(0·4·9)이 다 섰다 (2026-08-08).** 전곡 작곡의 첫 기둥이고, 뒤의 아홉 악장이 되받을 원형이다. 상세는 `05` 9.13절 · `07` 12장.

**다음은 1.4.4(4악장 세비야, 둘째 기둥)다.** `01` WBS 1.4 표의 선행이 1.4.1이었고 그것이 풀렸다.

> **1.4에 규칙이 하나 생겼다 (T-07) — 악장 하나를 고치면 바로 다음 악장까지 함께 잰다.**
> v1.13의 `mark(seed=)`가 난수는 끊었지만 `prev=LAST`가 화성을 잇는다. 오늘 0악장만 고쳤는데 1악장이 −28.4 dB 움직였다. 설계이므로 끊지 않고 절차로 막았다. **"한 악장만 고쳤으니 그 악장만 보면 된다"가 아니다.**

**BL-29(스템 분리)·BL-30(렌더 재현성)을 1.4 착수 전에 끝냈다 (v2.1).** 믹스 구조가 바뀌면 전 악장의 숫자가 다시 움직이므로 작곡이 시작된 뒤에 하면 판별할 수 없게 된다 — 오늘 아침 전역 난수를 끊은 것과 같은 논리다(`97. 회고` T-01·T-05·T-06). **이제 믹스 구조가 최종이고 렌더가 비트 단위로 재현된다.**

산출된 음원·영상:

| 파일 | 무엇 |
|---|---|
| **`0악장 B판 - 38초에 주제 한 번 더.mp3`** | **1.4.1 판정 대상 · 채택됨** (v2.4). 짝인 A판(침묵 유지)은 미채택 |
| **`전곡 코드 진행 v1.4 - BL-25 베이스 대선율 수작업 9분40초.mp3`** | **G2 판정 대상 · 승인됨** (v1.13, 320kbps) |
| `전곡 코드 진행 v1.4 - 성부 진행 최적화 9분40초.mp3` | v1.5 납품물. 옛 무그 필터·알고리즘 베이스 |
| `전곡 코드 진행 v1.4 - 화성 검토 영상.mp4` | 유튜브 업로드용. CQT 음정축 + 화음 심볼 263개. **v1.5 음원 기준** |

최근 실측값 (재현 시 이 값과 비교):

| 지표 | 값 |
|---|---|
| 외성 병행 5도 · 8도 | **0건 / 0건** (화음 349개) |
| 평균 총이동량 | 3.93 반음 / 화음전환 (4성부 합) |
| 경직됨 지표 (2~4kHz 포락선 변동계수) | **0.941** — v1.3은 1.185("조잡하다"), 승인받은 건반 스케치는 0.916 |
| 화음 라벨 ↔ 실제 울리는 음 | 88.9% (4악장만 66.2% — 타악 잡음이 크로마에 번지는 측정 한계) |
| 영상↔소리 지연 | 0 샘플 (접합부 포함) |
| 조성 밖 음 합 · 악장별 | 0.086~0.277. **3악장 0.092 · 8악장 0.166** — `05` 9.9절이 정본 |

> **2026-08-06 재실측 주의** — 위 표는 v1.5·v1.6 납품물 기준이다. BL-24b 신규 무그 필터가 들어간 현재 작업본을 재렌더하면 경직됨은 **0.943**, 7악장 RMS는 **−18.2 dB**로 미세하게 움직인다(성부 진행 통계는 349개·3.93반음·병행 0건으로 완전히 동일). **`05` 문서 9.2절 표의 3·8악장 값은 재현되지 않았다** — 경위와 정본 실측표는 9.9절에 있다.
>
> **위 주의는 v1.10에서 해소됐다.** 0.943·−18.2 dB는 BL-24b **작업 중간본**을 잰 값이었다. 새 필터가 무그 스템을 3.8 dB 죽이고 있었고, 그것을 `LEVEL_MATCH`로 되돌린 최종본은 **경직됨 0.941 · 7악장 −18.0 dB**로 v1.5와 완전히 같다. 악장별 RMS 열 개가 전부 일치한다. 상세는 `05` 9.10절.
>
> **v1.13 — 위 표는 이제 기준선이 아니다.** ffmpeg으로 v1.5 납품 mp3를 직접 실측한 결과 **9.2절의 재현 불가는 렌더가 아니라 기록의 오류로 확정**됐다(9악장까지 넷). 그리고 BL-25에서 **전역 난수가 악장을 결합하고 있던 것을 끊었으므로 1~9악장의 흔들림이 한 번 전부 달라졌다.** 현재 값은 경직됨 **0.955** · RMS 폭 **5.8 dB** · 9:26 F♯ **0.136**이고, 성부 진행 다섯 지표(349 · 3.93 · 병행 0/0 · 도약 0 · 밀집 1)는 불변이다. **정본은 `05` 9.11절.**

**승인된 결정 (2026-08-06, v1.8)** — 상세는 `07. 작곡 계획` 5·9장.

1. ✅ **1.4 작곡 순서 변경** — 순차(1→9)에서 **세 기둥(0·4·9) → 도시 → 연결 프롬나드**로. 전환은 양쪽을 알아야 쓸 수 있고, 모든 것이 피날레를 향해야 한다
2. ✅ **G2.5 중간 검수 게이트 신설** — 세 기둥이 선 시점에 한 번 본다. 골격이 틀렸다면 도시 악장 다섯 개를 쓴 뒤보다 훨씬 싸게 고친다. **`01` WBS 1.4.9로 반영, 임계경로 +1.5일(28.5→30.0)**

**착수 대기 (우선순위 순)**

| # | 무엇 | 비고 |
|---|---|---|
| ~~BL-32~~ | ~~시간에 따라 변하는 센드~~ | **완료 (v2.6)** — `send_env()`. 센드를 **잔향 앞에** 곱해 악장 경계에서 꼬리가 안 잘린다. 상수 센드는 예전과 −129 dB 차이라 회귀 없음 |
| ~~BL-24b~~ | ~~무그 필터 자기발진~~ | **완료 (v1.10)** — 되먹임 라더 `ladder_nl`. 발진 음정 3.9센트, 회귀 없음, 청취 판정 통과. **비명의 악장별 배치는 1.4 소관** |
| ~~BL-25~~ | ~~베이스 대선율 수작업 교체~~ | **완료 (v1.13)** — 0악장(걷기의 원형, 132개 손 작업) · 4악장(밀도 단계별로 앉는 자리) · 6악장(선율을 시간으로 나눠 듦, **`07` 4장 원안과 다르다**). 1·3·7·8악장은 1.4에서 편곡과 함께. 상세는 `05` 9.11절 |
| ~~WBS 1.4.1~~ | ~~0악장 Promenade 제시~~ | **완료 (v2.4)** — 보이싱 상한 `53~65`(목소리 자리) · 주제 6회차를 11박 격자의 45번 칸에 · 잔향은 걸지 않음. 상세는 `05` 9.13절 |
| ~~WBS 1.4.4~~ | ~~4악장 세비야~~ | **완료 (v2.6)** — **고칠 악보가 없었다.** 밀도 구조는 v1.3, 베이스는 BL-25 에서 확정. 실제 내용은 **공간**이었다 — BL-32 로 무그 잔향 곡선(4악장 0.15 실재 · 7악장 1.00 · 8악장 0.70 · 9악장 0.45). 상세는 `05` 9.15절 · `07` 13장 |
| ~~WBS 1.4.8~~ | ~~9악장 The Great Gate~~ | **완료 (v2.7)** — 걸음 위에 컴파스를 겹치고 **566초 F♯→F 에서 컴파스가 물러난다.** 무그 벨팅(비브라토가 차오른다) · **비명은 8악장 소관으로 자리를 정했다.** 9:26 F♯ **0.167**. 상세는 `05` 9.16절 · `07` 14장 |
| **WBS 1.4.9** ★ | **G2.5 중간 검수 게이트** | **다음 작업 — 검수자 판정.** 세 기둥(0·4·9)이 다 섰다. 볼 것 둘: **4악장 베이스 부재가 느껴지는가 · 9악장에서 두 주제가 하나였다는 게 느껴지는가**(`07` 7장). 여기서 걸러지면 도시 악장 다섯 개를 잘못된 골격 위에 쌓지 않아도 된다 |
| ~~BL-29~~ | ~~`Desk` 스템 분리~~ | **완료 (v2.1)** — 세 단계. 구조만 교체(−128 dB) → `mix(sends=)`·`plate()` → `믹스.py`. **BL-28의 선행이 풀렸다** |
| ~~BL-30~~ | ~~렌더 재현성~~ | **완료 (v2.1)** — 씨앗 없는 난수 때문에 **렌더할 때마다 소리가 달랐다.** 지금은 100.0000% 비트 일치. `97. 회고` T-06 |
| BL-31 | 남은 무씨앗 난수 여섯 곳 | 드럼·`bass()`. 지금 곡엔 안 쓰이지만 **1.1.6 드럼 재설계에서 살아난다** |
| **BL-28** | **여행자 보컬 PoC** | 노랫말 결정에 따른 신설(v1.15). 외부 도구로 11음절 한 프레이즈를 뽑아 주제 A에 얹어보고 판정. 음색·약관·**재현성**·비용 넷을 본다. **음색 판정 기준은 로우미드 200~500Hz 실측**(v1.17). 통과 못 하면 무가사로 되돌린다 |
| BL-27 | 9:26 F♯ 판정 방법 재설계 | 1.4.8 수락 조건이 **자기 측정 흔들림보다 작은 차이**를 재고 있다(씨앗만 바꿔도 0.119~0.136) |
| BL-26 | 스킬 문서의 수치 사본 정합 검사 | `검증.py` 7번째 검사. 5절 규칙을 자동으로 지키게 한다 |
| BL-23 | 현악 비중 | 6악장 재설계로 요구가 줄었다. 현재 피아노 대비 −18 dB |
| BL-05 · BL-21 | 보칼리즈 | 7악장은 무그로 확정. 사람 목소리를 쓸지는 미결 |
| BL-22 | 마드리드 13장에 "빈 의자" 사진이 실제로 있는지 확인 | 마지막 컷의 전제 |

---

## 8. 작업 방식

- **작업 근거는 프로젝트 안에 둔다** (v3.2). 임시 폴더는 **버려도 되는 것**만 — 전송용 사본, 폐기할 초안. **예상값 표와 분석 스크립트는 근거이므로 프로젝트에 넣는다.** 2026-08-08 에 `05` 9.15~9.17과 T-07이 인용하는 예상값 표 넷이 임시 폴더에만 있었다. **"손대기 전에 예상을 적는다"는 규칙의 값은 그때 적었다는 것이 증명되는 데 있는데, git 밖에 있으면 증명이 안 된다.** 예상값은 `98. 일일 마감/YYYYMMDD-HHMM-expectation-<주제>.md`, 분석 스크립트는 `99. 작업 스크립트/`
- **긴 렌더는 시간을 알려주고 시작한다.** 전곡 렌더는 2코어에서 4~5분이었다
- **음악은 귀가 아니라 숫자로 먼저 검증한다.** 크로마·온셋·대역·성부 이동량을 실측하고 문서에 값을 남긴다. "좋아졌다"가 아니라 "1.185 → 0.941"로 말한다
- **제약을 컨셉으로 바꾼다.** 이 프로젝트의 좋은 결정은 대부분 그렇게 나왔다 — 저해상도 → "상상 속 인물에게는 얼굴이 없다", 약한 현악 → "비움으로 만드는 광활함", 사람 목소리 부재 → "언어 없는 목소리에는 신디사이저가 맞다", 음집합 보존 → "E♭ 리디안"
- **틀렸으면 인정하고 원문을 남긴다.** 개정 절에 무엇이 왜 바뀌었는지 적는다(R3)
- 검수자는 **프로그레시브 록**을 좋아한다. 기준은 **Renaissance 〈Mother Russia〉**(주), ELP 《전람회의 그림》(변주 원천). 1970년대 신디사이저 소리를 좋아하지만 **그것이 조잡한 소리를 받아들인다는 뜻은 아니다**

### 검수자에게 설명하는 법 (R7 · v2.2)

**검수자는 음악적 판단은 정확하지만 음향·미디 전공이 아니다.** "조잡하다", "베이스가 중심임", "템포 느림" 같은 지적은 전부 맞았다. 반면 **렌더·리버브·스템 같은 제작 용어는 배경이 아니다.** 2026-08-07에 "렌더가 무엇인지 모르겠다"는 말을 직접 들었다.

**용어가 막히면 판정 자체가 막힌다.** 이건 검수자의 공부 부족이 아니라 내 전달 실패다.

**규칙 셋**

1. **전문 용어는 처음 쓸 때 그 자리에서 푼다.** 링크로 미루지 않는다. 한 줄이면 된다 — "리버브(울림. 욕실에서 말하면 웅웅거리는 그것)"
2. **비유를 하나 붙인다.** 이 프로젝트에서 통한 것들 — 렌더 = "악보를 소리로 굽는 것", 스템 = "악기별 트랙", 단일 버퍼 = "사진 253장을 한 장으로 인화해 놓고 저 사람만 밝게 해달라는 것"
3. **음원을 보낼 때는 반드시 아래 표를 붙인다.** 표 없이 파일만 보내면 무엇을 들어야 할지 모른다

**음원 설명 표 — 이 네 칸을 채운다**

| 구간 | 음향적 특징 | 공간 배치·효과 | 청취 포인트 |
|---|---|---|---|
| A (0:00~) | 무엇이 다른가 (한 낱말로) | 왜 그렇게 들리는가 | **어디를 들을 것인가** |

- **「구간」** — 시각을 반드시 적는다. 검수자가 그 지점으로 바로 갈 수 있어야 한다
- **「음향적 특징」** — 한두 낱말로 성격을 못박는다. 건조함 / 여운 / 앞뒤 대비
- **「공간 배치·효과」** — 무엇을 어떻게 했는지. 숫자가 아니라 그림으로. "무그만 큰 홀에, 나머지는 그대로"
- **「청취 포인트」** — **가장 중요한 칸.** "소리가 끊긴 직후를 들으세요" 같은 구체적 지시. 이게 없으면 표가 소용없다

### 숫자 표를 낼 때는 풀어쓴 판을 나란히 (v3.3)

**R7의 확장이다.** 표는 정확하지만 읽는 사람이 그 숫자가 무엇을 뜻하는지 알아야 쓸모가 있다. 검수자가 직접 요청한 형식이다.

| 그대로 쓰면 | 함께 붙일 것 |
|---|---|
| `♩ = 138` | **가볍게 조깅할 때의 속도.** 8분음표가 아니라 4분음표 기준이라, 쪼개서 적으면 악보가 두 배 빨라 보인다 |
| `화성 리듬 1.67초마다` | **1.7초마다 소리의 색깔이 바뀐다.** 지루할 틈이 없다 |
| `RMS −16.9 dB` | **적당한 음량.** 빵빵하게 키우지 않고 크고 작음의 차이를 남겨둔 상태 |
| `드럼 −49 dB` | **아주 살살 친다.** 쿵쾅거리는 게 아니라 묵묵히 박자만 맞춰주는 역할 |

**쓰는 법 셋**

1. **비교로 말한다** — "♩=138"이 아니라 "조깅 속도". 숫자를 아는 사람만 아는 말로 끝내지 않는다
2. **역할로 말한다** — "−17 dB"가 아니라 "주인공". "−49 dB"가 아니라 "숨은 조력자"
3. **함정을 미리 말한다** — ♪와 ♩를 헷갈리면 악보가 두 배 빨라진다 같은 것

`악장표.py`가 두 판을 함께 찍는다. **규칙을 문서에만 적으면 안 지켜지므로 도구가 하게 한다.**

**그리고 무엇을 정해야 하는지 한 문장으로 끝맺는다.** "0.5로 충분한지, 1.0쯤인지만 말씀해주시면 됩니다." 검수자가 답할 것이 무엇인지 분명해야 한다.

용어 풀이는 [`97. 회고/00. 용어 사전`](<97. 회고/00. 용어 사전.md>)에 쌓는다. 새 용어를 쓰기 시작했으면 거기 항목을 추가한다.

---

## 9. 하지 말 것

- 문서 내용 삭제 (R3)
- 파일명에 언더바 (R2)
- 음집합 밖의 음을 끌고 오는 화음 추가 (6절)
- **1악장에 F♯** — 그 악장의 서사는 "혼자"다
- 4악장에 생성 영상 (V3), 그녀의 얼굴 클로즈업 (V1), 배경 변경 (V2)
- ELP·호로비츠 편곡 참조 (6절)
- `sketch.py`·`demo60.py`·`themes_v12.py`·`themes_v13.py` 수정 — 과거 버전 기록이다
- **G3 PoC 통과 전에 사진으로 영상 만들기** — Phase 3 작업을 앞당기면 버리는 일이 된다
- 문서를 고치고 `검증.py`를 안 돌리기 (R5)

---

*최종 갱신 2026-08-08 · 프로젝트 v3.4*
