# 데일리보고 2026-08-06

**검수자가 읽고 판단할 것만 적는다.** 경위와 실측 전문은 [세션로그](<20260806-1737-session-log.md>)에 있다.

| 항목 | 내용 |
|---|---|
| 프로젝트 버전 | **v1.7 → v1.11** (네 단계) |
| Phase | 1 음악 · G1 승인 완료 · G2 데모 승인 대기 |
| 전곡 렌더 | **5회** |
| 검증 상태 | `검증.py` 통과 · 전 문서 v1.11 · 스냅샷 일치 |

---

## 1. 오늘 끝난 것

**BL-24b 무그 필터 자기발진 — 완료. BL-24 종결.**

옛 필터는 되먹임이 없어 자기발진이 **원리적으로 불가능**했다(입력 0에서 RMS 0.00000000). 되먹임 라더를 새로 만들어 필터가 스스로 울게 했고, 발진 음정 오차를 3.9센트까지 잡았다. 옛 필터와 출력 레벨을 0.1 dB 이내로 맞춰 **경직됨·프레즌스·악장별 RMS 열 개가 전부 v1.5와 같다** — 회귀 없음.

청취 판정 **"구별 안 됨 → 통과"** 받았다. 음색을 바꾸지 않으면서 능력만 얻는 것이 목표였으므로 구별되지 않는 것이 통과 조건이었다.

**작곡 순서 변경 · G2.5 게이트 — 승인 반영 완료.** WBS 1.4를 세 기둥 우선으로 재배열했다.

**문서 실측표 오류 두 건 정정** (`05` 9.9절) · **`검증.py` 버전 정렬 버그 수정.**

---

## 2. 검수자가 알아야 할 것

### 총 기간이 28.5 → 30.0 작업일로 늘었다

작곡 순서 변경의 대가다. `07` 문서 5장이 "작업량은 순차와 같다"고 썼는데 **캘린더 기준으로는 틀린 서술이었다.** 0악장과 2·5악장이 여유 작업에서 순차 작업으로 바뀌면서 임계경로에 올라온다. 여기에 G2.5 게이트 0.5일.

이미 승인하신 결정의 결과이므로 되돌릴 사안은 아니지만, **일정이 1.5일 늘었다는 사실은 알고 계셔야 한다.**

### 납품물이 현재 코드와 다르다

`전곡 코드 진행 v1.4 - 성부 진행 최적화 9분40초.mp3`와 검토 영상은 **옛 필터로 만든 것**이다. 지금 코드가 내는 소리와 다르다. 다시 만들려면 **ffmpeg가 필요한데 이 PC에 없다.**

"구별 안 됨" 판정이 나온 만큼 급하지는 않다. 다만 G2 데모 승인을 이 mp3로 하실 거라면 **판정 대상과 현재 코드가 어긋난 상태**라는 점을 감안해야 한다.

### 문서의 v1.5 실측 기록이 세 번째로 재현되지 않았다

3악장·8악장에 이어 **피날레 9:26**도 문서값(F♯ 0.149)이 옛 필터로도 재현되지 않았다(0.135). `05` 9.9절이 원인을 확정 못 한 채 남겨둔 문제인데, 사례가 셋이 되면서 개별 오타로 보기 어려워졌다. **`05` 9장의 실측값 전반을 기준선으로 쓸 때 주의가 필요하다.**

---

## 3. 미결 · 이월

| 항목 | 상태 |
|---|---|
| **BL-25 베이스 대선율 수작업 교체** | **1.4 착수 전 남은 유일한 선행 작업** |
| 비명(`scream`)의 악장별 배치 | WBS 1.4 소관. 지금 곡에는 한 번도 안 들어가 있다 |
| 1.4.8 수락 조건 | 9악장 재작곡 시 **9:26 F♯ ≥ 0.135** 확인 |
| `05` 실측 기록 재현 불가 | 원인 미확정. 사례 3건 |
| ffmpeg 미설치 | mp3 재생성·`review-video` 스킬 전체가 막혀 있다 |
| BL-23 현악 비중 / BL-05·21 보칼리즈 / BL-22 빈 의자 사진 | 종전대로 |

---

## 4. 다음 작업

**BL-25 → WBS 1.4.1 (0악장, 첫 기둥).**

BL-25는 현재 알고리즘 생성인 베이스 대선율을 악장별 의도가 있는 손 작업으로 바꾸는 일이다. 4악장(걷기를 멈춤)과 6악장(선율을 나눠 듦)이 핵심이다. 이것을 1.4보다 먼저 하는 이유는 무그 음색과 같다 — **베이스가 곡의 중심이므로, 중심이 정해지기 전에 편곡을 짜면 다시 짜게 된다.**

---

## 5. 검수 요청

없음. 오늘 요청드린 판정 두 건(작곡 순서·G2.5, 무그 음색)은 모두 승인받았다.

다음 검수 지점은 **BL-25 완료 후 세 기둥 작곡 → G2.5**다.

---

*작성 일자: 2026-08-06*
