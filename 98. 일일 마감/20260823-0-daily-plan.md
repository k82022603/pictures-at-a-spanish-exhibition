# 오늘 할 일 (2026-08-23 일 · 14:42 시작)

## Context

**어제 Suno 판 전곡이 납품 다섯 형식까지 나왔습니다** — 전곡 E · 10:19.84 · wav(float32) · FLAC 32/24비트 · MP3 · MP4. 어제 데일리보고 4절이 다음 작업으로 **WBS 2.2 「AI 영상 PoC 실행·판정」(G3)** 을 지목했고, 선행(`0.6` 기획 승인 · `2.1` 사진 셀렉)이 둘 다 찼습니다.

**그런데 상태를 확인하니 두 가지가 걸립니다.**

| | |
|---|---|
| **① 어제 Drive 업로드가 91% 에서 끊겼습니다** | 5.26 GiB 중 4.81 GiB · **96개 중 63개.** 8/22 폴더는 **로컬 114개 중 Drive 에 2개**뿐입니다. **전곡 E 마스터(227 MB)와 검토 영상(140 MB)이 로컬에만 있습니다** |
| **② G3 는 검수자 결정 둘을 기다립니다** | **비용 승인**(상한 $20)과 **어느 생성 도구를 쓸지.** Claude 는 결제를 못 합니다 |

**그래서 오늘은 「승인 없이 갈 수 있는 데까지」 갑니다.** 그리고 그 자리가 마침 이 프로젝트가 가장 여러 번 데인 곳입니다 — **자를 안 재고 쓰는 것.**

> 어제만 세 번이었습니다. `악장표.py`·`자막생성.py`·`영상검증.py` 가 조용히 「우리 곡」을 가정하고 있었고, `영상검증.py` 는 **닮은 정도 0.09** 로 「동기 어긋남」 다섯 줄을 찍었는데 **동기가 아니라 다른 곡과 견주고 있었습니다.**

---

## 오늘의 목표

> ### **G3 판정에 쓸 「자」를 먼저 만든다. 생성물이 오면 그날 바로 판정할 수 있게.**

**곡은 한 음도 안 건드립니다.**

---

## 본체 — `PoC검증.py` 신설

**[`02. AI 영상 생성 기술 검증 계획서`](<02. AI 영상 생성 기술 검증 계획서.md>) 4장이 정량 지표 넷을 못박아 뒀고, 넷 다 생성물 없이도 만들 수 있습니다.**

| 지표 | 무엇을 재나 | 합격선 (정본은 `02` 4장) |
|---|---|---|
| 구도 보존 | 생성 첫 프레임 vs 원본 **SSIM** | ≥ 0.90 |
| 직선 구조 | **Hough 직선 검출 개수** 변화율 | ±15% |
| 시간 일관성 | 인접 프레임 **옵티컬 플로우** 표준편차 | 급변 없음 |
| 노이즈 증폭 | 어두운 영역 표준편차 변화 | 1.3배 이내 |

### 왜 오늘인가

**생성물이 오고 나서 자를 만들면 늦습니다.** 그때는 「이 값이 맞나」를 판단할 기준이 없어서, **자가 틀렸는지 생성이 나빴는지 못 가립니다.** 어제 `구조분석2.py` 가 정확히 그 문제였고, **자검사 8/9 를 통과하고서야 쓸 수 있었습니다.**

그리고 **G3 는 8개 중 6개 통과가 합격선**입니다. 판정이 숫자로 갈리는 구조라 **자가 흔들리면 게이트 자체가 흔들립니다.**

### 자검사 — 답을 아는 문제로 잰다

**생성물이 없어도 검사할 수 있습니다.** 원본 사진만으로 답을 아는 경우를 만들면 됩니다.

| 넣는 것 | 나와야 하는 값 |
|---|---|
| **원본 vs 자기 자신** | SSIM **1.000** · 직선 변화 **0%** · 노이즈 변화 **1.0배** |
| 원본 vs **가우시안 블러** | SSIM 뚜렷이 하락 · **직선 검출 감소** |
| 원본 vs **잡음 추가** | **노이즈 지표가 1.3배를 넘김** (불합격을 실제로 찍는가) |
| 원본 vs **좌우 반전** | SSIM 크게 하락 (구도가 바뀐 것을 잡는가) |
| 정지 영상(같은 프레임 반복) | 옵티컬 플로우 **≈ 0** |

**미달이면 `sys.exit` 로 멈춥니다** — `구조분석2.py` 와 같은 방식입니다.

### 무엇으로 끝났다고 판정하나

**자검사 다섯이 전부 통과하고, T1~T6 원본 여섯 장에 실제로 돌려 기준선 값이 찍히면** 끝입니다. 생성물이 오면 그 값과 견주기만 하면 됩니다.

### 막히면

**옵티컬 플로우가 어려우면 그 지표만 뒤로 미룹니다** — T7(영상)에만 쓰이고 나머지 일곱은 정지 이미지라 안 걸립니다. `scipy` 만으로 안 되면 블록 매칭으로 근사하고, **그 사실을 도구 머리에 적습니다.**

---

## 곁가지 셋

### ① Drive 업로드 마무리 (백그라운드 · 20~40분)

**어제 마감이 미완인 채로 끝났습니다.** `rclone copy` 는 이미 올라간 것을 건너뛰므로 그냥 다시 돌리면 됩니다. **실패 원인(exit 4)이 로그에 안 남았으니 이번엔 로그를 파일로 받습니다.**

**끝났다고 판정하는 것** — `rclone ls` 로 8/22 폴더가 **로컬 114개와 같은 수**가 되는 것.

> **`96. 운영` 가이드가 "산출물은 Drive 가 갖는다"고 못박았는데 지금 전곡 E 마스터가 로컬에만 있습니다.** 본체보다 먼저 걸어둡니다.

### ② 테스트 사진 여덟 장 준비 — 실물 확인은 끝났다

**일곱 개가 다 있고 해상도도 `02` 3장과 일치합니다** (T1 2048×1536 · T3 1024×768 · T5 768×1024 · T7 MOV). **T8 만 준비가 필요합니다** — `02` 3장이 지정한 넷에 같은 실루엣을 합성하는 것이라 **실루엣 원본이 먼저**입니다(WBS 3.7).

**오늘은 T8 을 만들지 않습니다.** 무엇이 필요한지만 적어 둡니다.

### ③ 도구 선택지 정리 — 검수자 결정용

**어느 생성 모델을 쓸지가 안 정해져 있습니다.** `02` 5장의 프롬프트 원칙은 도구 중립적으로 쓰여 있어 **선택지를 표로 정리하면 검수자가 바로 고를 수 있습니다.**

| 정리할 것 |
|---|
| 이미지→영상 생성을 하는 서비스와 **각각의 과금 방식** |
| **PoC 8건에 실제로 얼마 드는가** (상한 $20 안에 드는가) |
| 저해상도(1024×768) 입력을 받는가 · 업스케일이 먼저 필요한가 |
| **상업적 이용·크레딧 의무** — `CLAUDE.md` 저작권 절이 요구하는 확인 |

**이건 조사이고 결제가 아닙니다.** 계정 생성·결제 직전에서 멈춥니다.

---

## 오늘 하지 않는 것

| 무엇 | 왜 |
|---|---|
| **실제 영상 생성** | **비용 승인과 도구 선택이 안 됐습니다.** 계정 생성·결제 직전에서 멈춥니다 |
| **사진으로 뮤직비디오 만들기** | `CLAUDE.md` 9절 — **G3 통과 전 금지.** 앞당기면 버리는 일이 됩니다 |
| **전곡 E 유튜브 업로드** | **검수자가 아직 전곡을 통으로 안 들으셨습니다.** 판정 전에 올리지 않습니다 |
| **전곡 E 손대기** | 판정 대기. 6악장 론다는 **원본으로 확정**됐습니다(`20` 6절) |
| **2악장 경계 둘 확정** | 귀로 확인해야 하는데 **검수자 시간이 듭니다.** 전곡 E 판정하실 때 함께 여쭙는 편이 낫습니다 |
| **T8 실루엣 합성** | WBS 3.7(그녀 레퍼런스 실루엣)이 선행이고, **그것 자체가 2.2 통과를 기다립니다** |

---

## 검증

| 무엇 | 어떻게 |
|---|---|
| **자** | **자검사 다섯 전부 통과.** 미달이면 도구가 멈춘다 |
| 기준선 | T1~T6 여섯 장에 돌려 값이 찍힌다 |
| Drive | `rclone ls` 로 **8/22 폴더가 로컬과 같은 수** |
| 문서 | 도구를 만들었으면 **`16` 갈래 표에 등재**(어제 세운 규칙) · `검증.py` 7종 |
| 곡 | **렌더 없음.** 전곡 E 그대로 |

---

## 하는 순서

1. **Drive 업로드를 백그라운드로 먼저 건다** (20~40분)
2. 오늘 계획을 `98. 일일 마감/20260823-0-daily-plan.md` 에 적는다
3. **★ 본체 — `PoC검증.py` 짓고 자검사 통과시킨다**
4. T1~T6 기준선 값을 찍는다
5. 도구 선택지를 표로 정리해 **검수자께 드린다**
6. Drive 완료 확인 · `16` 등재 · `검증.py`
